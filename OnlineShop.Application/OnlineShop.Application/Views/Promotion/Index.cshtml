@model IEnumerable<OnlineShop.Backend.Models.Promotions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DiscountValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DiscountValue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PromoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PromoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PromoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
